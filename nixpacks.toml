providers = ["node"]

[phases.setup]
aptPkgs = ["ffmpeg", "python3", "make", "g++", "build-essential"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
